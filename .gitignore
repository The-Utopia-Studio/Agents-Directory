.DS_Store
node_modules/
.vercel

# Never commit real secrets — but keep the .env.example templates tracked so
# the team knows which variables to set.
.env
.env.*
!.env.example
.cursor/skills/
convex/_generated/

# Phase 2 exports may contain catalogue/request identity data. Browser downloads
# normally land outside the repo; this catches deliberate local copies.
migration-output/
