# Keep shared authority deployments out of committed defaults.
CONVEX_URL=

# Vercel build input for the browser sign-in surface. This is a publishable
# `pk_…` Clerk key, not a Clerk secret key or JWT.
CLERK_PUBLISHABLE_KEY=

# Public Clerk issuer trusted by convex/auth.config.ts. Configure the same
# value on each Convex deployment; this is not a secret.
CLERK_JWT_ISSUER_DOMAIN=https://valid-collie-71.clerk.accounts.dev
CLERK_JWT_AUDIENCE=convex

# Loop → Convex evidence writes (state B). Deploy key is server-only; never
# put it in the browser. Recorded actor is service:agents-directory / agents-directory-loop.
# State D (intended): short-lived JWTs from that issuer, verified by Convex.
CONVEX_DEPLOY_KEY=

# Loop PR writer for The-Utopia-Studio/utopia-agents (private). Fine-grained
# PAT: Contents read+write and Pull requests read+write. Server-only.
# Never use for fellow skill downloads — that path lives in utopia-os and
# uses GITHUB_SKILLS_TOKEN (Contents:read only). No shared helper / fallback.
GITHUB_LOOP_TOKEN=

# Merged loop/ PR -> Convex release. See docs/MERGED_PR_RELEASE.md.
# HMAC secret for POST /api/github/webhook. This is the ONLY authentication on
# the one route that can move currentApprovedVersionId without a signed-in
# human — unset means every delivery is refused (503), never accepted unverified.
GITHUB_WEBHOOK_SECRET=

# TEMPORARY authority mechanism. Comma-separated GitHub logins allowed to
# release by merging a loop/ PR. Unset/empty/whitespace-only refuses EVERY
# release — an empty allowlist is never permissive. Compared case-insensitively
# and trimmed, otherwise exactly (no prefix matching).
# Intended end state: a GitHub -> Clerk approver mapping so one approver set
# governs both the UI approve button and the merge path.
LOOP_RELEASE_APPROVERS=
